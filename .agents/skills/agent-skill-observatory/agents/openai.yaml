interface:
  display_name: "Agent Skill Observatory"
  short_description: "Repo-specific patterns and workflows for agent-skill-observatory"
  default_prompt: "Use the agent-skill-observatory repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true